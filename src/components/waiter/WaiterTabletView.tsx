import React from 'react';

// Deprecated: Fryway operates on Counter/Takeaway + Home Delivery + Kitchen + Admin ecosystem.
export const WaiterTabletView: React.FC<{ onExitMode?: () => void }> = () => null;
